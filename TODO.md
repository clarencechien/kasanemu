# TODO

還沒決定、或決定了還沒做的事。做完的搬進 `docs/deviations.md` 或 `CHANGELOG.md`。

---

## 圖片加註的語彙 —— **暫緩,等決定**

比較台:`scripts/bench-vocab/`(mockup,附真圖與真譯文)。
量測見 `docs/plan-images.md` §13-7。

### 現在的規則(三層)

| 層 | 在哪 | 內容 |
|---|---|---|
| 逐塊「放得下嗎」 | `imageblocks.ts` | `fs = min(框高×0.8, √(框寬×框高/(字數×1.35)), 40)`,`≥ 11px` 算放得下 |
| 整張圖「用哪種」 | `imagegeo.ts` | 七成以上放得下 → 疊字,否則錨點(`VEIL_MAJORITY`) |
| 每種長什麼樣 | `overlay.ts` | 疊字 = 毛玻璃 + 譯文貼片;錨點 = 圓點 + 貼片 + 放大檢視的清單 |

### 比較台量出來的三件事

1. **遮蔽面積與互動次數單獨看都會騙人。** 密集素材上強制疊字是
   「遮蔽 17.2% · 0 次互動」——兩欄都漂亮,而畫面完全不能看。
   分得出好壞的是第三個數字:**標籤重疊 / 線交叉**。
2. **拉線是唯一「稀疏時明顯最好、密集時明顯最爛」的做法。**
   565px 的圖表:七條同時可讀、零互動、線一處不交叉。
   同一套邏輯放到 53 塊:78 處交叉、27 條掉出畫布、遮蔽 40%。
   它不是備案,是**另一個適用帶**。
3. **同一張圖 L0 與 L1 會給不同語彙。** `gemma-chart` 與 `lite-chart`
   是同一張圖,1040px 時一個 86% 走疊字、一個 43% 走錨點 ——
   所以 Alt+click 升級可能讓整張圖的視覺語言當場翻面。

### 量出來的第四件事(2026-08-27,`npm run measure:vocab`)

**多數決該換掉,而換成什麼已經量出來了**(`docs/plan-images.md` §13-9)。

`譯文佔版`(貼片總面積 ÷ 圖面積)是唯一同時抓得到「疊在一起」和
「壓不到但滿滿都是」的量尺,而且隨顯示尺寸單調 ——
圖表家族 2–10%,截圖家族 10–82%,中間隔一個數量級。

它一個數字回答三件事:加到預算為止 = 翻多少;連最大的一塊都放不下 = 走錨點。

**還沒決定的是產品面**:模擬顯示錨點會幾乎消失(總有夠小的塊塞得進預算)。
與其在縮圖上畫 47 個圓點,不如疊最重要的 7 塊、其餘留給放大檢視 ——
這是取捨不是量測,要先看得到才決定得了。**下一步是把它畫出來比一比**
(`render-veil.mjs` 已經有目視稿的骨架)。

### 候選做法與 effort

| | 做什麼 | Effort | 解決 |
|---|---|---|---|
| A | 調數字(11px / 70%) | 30 分鐘 | 移動懸崖的位置,懸崖還在 |
| B | 多數決改成面積加權 | 1 小時 | 大標籤主導,footnote 不拖累 |
| **F** | **語彙鎖定**:第一次決定後跟著那張圖走 | 1 小時 | 上面第 3 點 |
| **D** | **chip 加手動切換**,自動規則只當預設 | 半天 | 全部——不必猜對,只要預設不離譜 |
| G | 送模型前後**合併相鄰小框** | 1–2 天 + 風險 | 密集素材的根因(粒度) |
| E | 行內也給側邊清單 | 1 天 | 和放大檢視功能重疊 |

**傾向**:先 F(純粹是 bug 級改善),再認真考慮 D。G 是對的方向但要先量準確度。

**已經順手做掉的**:候選 A 的一半 —— 「譯完等於沒譯 / 數字加單位」的塊
根本不畫,也不參與語彙投票(§DP-2、§DQ-2)。密集素材的塊數因此少一截,
懸崖的位置動了,但懸崖還在,上面六案都還算數。

---

## 字很多的圖要不要先擋掉 —— **先維持現狀(C),之後再調**

整頁截圖 / 手機截圖那種塞滿字的圖,輸出長到跑不完 100 秒的時限(§DS-2)。

**現在的行為(C)**:完整地問 → 逾時 → 圖角說「點一下只翻大字」→
使用者點一下,改成只要最顯眼的 12 塊。最不會錯殺,代價是踩到的時候要等 100 秒。

| | 做什麼 | 代價 |
|---|---|---|
| A | 圖夠大(例如 natural > 2.5MP)第一次就用 brief 問,chip 註明「大字版 · 點一下翻全部」 | 永遠不逾時,但要先量門檻該下在哪 |
| B | 圖夠大就直接不翻,chip 說「這張圖字太多,不翻」 | 最省配額,錯殺的時候使用者只能自己想辦法 |
| **C** | **維持現狀** | 最不會錯殺,踩到要等 100 秒 |

**沒有選 A/B 的理由**:「字很多」這件事**只有問完模型才知道**。
送出去之前手上只有像素尺寸,而大圖既包含整頁截圖,也包含資訊圖表、
投影片、產品說明圖 —— 用尺寸擋會連想翻的一起擋掉,而且擋掉的時候
使用者不知道為什麼。

要動 A 的話**先量**:找一批實際會滑到的圖,對照 natural 尺寸與模型回的塊數,
看門檻分不分得開。沒量出分得開的門檻就不要下。

---

## 站方 lightbox 與我們自己的框

已確認**不接管點擊**(會破 `pointer-events: none` 與 §2.4「跟著站方走」),
但兩邊的加註語彙要做到一模一樣。目前站方 lightbox 靠同 src 認親跟過去,
放大檢視是我們自己的框——**兩邊的樣式尚未對齊**。

側邊清單在站方 lightbox 裡不可行(會蓋到它的關閉鈕),
所以「一模一樣」這件事本身有上限,需要再想。

---

## 偵測:爬站稽核剩下的幾類

`node scripts/audit-tree.mjs <url>` 爬完 thariqs.github.io 之後還剩的,
都是**短的**,所以沒有繼續追。滑上去仍然翻得到,只是不畫疊層:

- `<header>` 裡是模擬的 GitHub PR 介面(h1 45 字、其餘全是短欄位)
- `<aside>` 裡一句話被 inline 元素切成三段,每段都不到 80 字
- 「符合所有規則卻沒成為單元」15 段 —— 稽核的解釋和 walk 的實際行為
  不一致,要查的是**稽核**還是 detect 還不確定

## 量測還缺的素材

- 人工標註的框準度(`docs/plan-images.md` §13-2 要的那個)
- 真正的手持照片(§13-3 的但書)

---

## 文件對齊

`npm run audit:docs` 現在守著**機械上驗得出來**的那部分:§ 編號、
文件裡寫的 npm 腳本名、檔案路徑、README 說的測試數、deviations 有沒有跳號。
它進了 `npm run check`。

守不住的是**散文有沒有過時** —— 那要人讀。這一輪人工掃出來的四處
(plan-images 說「尚未實作」、plan-annotation 說「圖片是下一步」、
feature.md 說「待啟動」、README 說「Phase 2 圖片翻譯不做」)都改了,
但下一次還是只能靠人。想得到的自動化只有「狀態行必須是一組固定字串」,
還沒做。
