# Changelog

## v0.1.0 — 文字階段收關

自用的 Chrome 疊層翻譯:譯文以不透明疊層覆蓋原文,**完全不改動頁面 DOM 與 CSS**。

### 這一版有什麼

- **兩段式翻譯**:L0(Chrome Translator API,本機、零成本)先填滿畫面,
  停留超過 1.5 秒的區塊才升級到 L1(Gemini / Gemma),就地 cross-fade 替換,
  不動幾何、不重算字級。
- **三層防守**:內文疊翻、UI 標籤走 hover / 選取貼片(不覆蓋原文)、
  `code` 與 `.notranslate` 一律不翻。沒有元素可掛的鬆散文字與被行內圖片
  切開的段落改用 `Range` 錨點,而且**疊層絕不蓋到圖**。
- **詞表**:`原文 → 譯法`,只寫原文 = 不翻。全域 + 具名詞表 + 網域 pattern
  綁定。做法是私用區佔位符 —— 模型從頭到尾沒看到那個詞,所以**每個檔位
  都有效,連 L0 也有效**。有自己的匯出匯入(和快取那組分開)。
- **成本閘門**:不按就不翻、停留才升級、每頁 token 上限、每日預算、
  token bucket、429 退避。thoughts 單獨計價列出。
- **看得見的失敗**:提示線階層色、頁內狀態列、診斷 log 一鍵匯出
  (API key 遮罩、字串截斷到 60 字)、L1 佇列看門狗讓「卡住」不是一個
  能永久停留的狀態。
- **匯出**:譯文快取(JSON)、疊好的頁面(HTML,hover / Alt 仍可看原文)。

### 品質

- 217 個測試 + 三支真瀏覽器 probe(選取規則、顏色解析、HTML 快照)。
- 37 個台灣讀者常看的美日內容站與 CS 技術站的批次偵測稽核
  (`npm run audit:sites`)當回歸測試。
- 走過的坑逐件記在 `docs/deviations.md`,歸納出的通則在 `docs/lessons.md`。

### 限制與下一步

- **圖片翻譯不做**,進場條件是文字階段連續用兩週。
- 導覽列、頁尾、表單裡的文字不畫常駐疊層(hover / 選取翻得到)—— 這是設計。
- 日文 / 韓文是「應該會動」而不是「測過了」,主力測試對象是英文。
- L0 只在桌機版 Chrome 138+ 且語言包裝得起來時存在;不支援會自動退回 `single`。

### 安裝

下載 zip、解壓,`chrome://extensions` → 開發人員模式 → 載入未封裝項目 → 選解壓出來的資料夾。
使用說明在擴充功能內的手冊(popup → 手冊)。
