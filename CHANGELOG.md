# Changelog

## v0.2.0 — 圖片加註

圖片上的字不是文字節點,是像素。疊層碰不到它們,所以圖片走另一條路。

> **改**:圖片加註**只蓋真的翻了的**。數值、產品名、代碼、軸標籤這些
> 「翻完和原文一樣」的,現在原封不動留著原圖 —— 一張長條圖實測從 18 個
> 加註掉到 3 個,剩下的原文清清楚楚。
>
> **修**:某些網站(The New Stack 就是)**整篇內文的譯文都不見**,只有標題
> 翻得出來 —— 而且統計顯示「63 塊翻好 60 塊、零失敗」。翻譯確實做完了,
> 是疊層畫上去之後被自己藏掉的:那些站用 `body { overflow-x: hidden }`
> 擋橫向捲軸,我們誤判成「body 會裁切內容」,於是**首屏以下的每一段
> 都被當成看不見**。實測那一頁修之前 73 段有 68 段被藏,修之後 0 段。
>
> **修**:一次滑過好幾張圖的時候,**除了第一張以外幾乎都會「等太久已取消」**。
> 免費檔一次只跑一張、一張要十幾到四十秒,而舊規則是「排隊超過十秒就丟」
> —— 排在後面的必定超時,和你想不想看完全無關。現在改成用**張數**擋
> (最多排 4 張,超過就讓最舊的位),排隊本身不再算逾期。
>
> **修**:圖片翻譯失敗時,圖角那句「點一下重試」**現在真的點得下去**,
> 而且點了會再送一次 —— 以前那句話沒有對應的按鈕,怎麼點都沒反應,
> 滑開再滑回來也一樣(失敗等於終點站)。不能重試的(額度用完、格式讀不了)
> 現在也不會再叫你點。
>
> **修**:字很多的圖(整頁截圖那種)逾時之後,重試改成**只翻最顯眼的
> 十來塊**(標題、大標、大字)。逾時不是模型當掉,是輸出太長 ——
> 實測一塊要 2.3 秒,100 秒的時限等於 43 塊,所以同一份請求再送一次
> 只會再等 100 秒。
>
> **修**:圖片還在「辨識中…」的時候 <kbd>Alt</kbd>+click 升級會被吞掉
> ——連結不走、請求也不送。現在送得出去。
>
> **修**:一張圖上兩個加註靠得近的時候,**後面那塊會蓋掉前面那塊的譯文**
> ——先是毛玻璃,拿掉之後換白暈。以前每一塊各自帶著自己的玻璃和白底,
> 誰後畫誰在上面。現在是三層:玻璃、白底、字。**譯文永遠在最上面,
> 只有字和字會互相擠。**
>
> **改**:加註讀起來是**一團雲**了。以前是「一圈灰環包著一條白貼片」,
> 中間兩層各自有邊;現在白暈放大到和霧接上,深底 → 霧 → 白 → 字
> 是一條連續的坡,沒有可以指認的分界。羽毛也大了三倍(8px → 26px)。
>
> **改**:加註的毛玻璃**不再是一個方框** —— 換成中性灰,四角和邊緣自己
> 淡出去,邊框、上緣高光、投影全部拿掉。譯文底下那片白也是羽化的,
> 沒有硬邊也沒有陰影。整片讀起來像玻璃在那裡比較厚,不像貼了一張紙。
> (白色本身拿不掉:量過,拿掉之後深色圖表上的譯文只剩 1.8:1;
> 而羽化幾乎不花錢,最差的原文殘留一分沒動。)
>
> **改**:`5x` `4x` `25GB` `100ms` 這種**數字加單位**也不蓋了 ——
> 翻成「5 倍」只換到一個字,而代價是把整個數字糊掉。有句子的照常翻
> (`5 tips`、`Top 10`、`19 times smaller` 都還在)。
>
> **修**:某些網站(Google 自家的部落格就是)的圖片一張都翻不了,
> 因為對方的伺服器把 WebP 標成「未知檔案」,而我們把那個標籤原樣轉給了
> 模型。現在**看檔案本身**決定格式,不看伺服器怎麼說;模型收不了的格式
> 會自動轉檔。順帶:API 的錯誤訊息現在看得到重點,不會被 JSON 外殼截掉。
>
> **修**:`<header>` / `<aside>` / `<footer>` 裡裝著真正內容的,以前
> 整塊會被當成網站外殼跳過 —— 標題、側欄的結論段落、頁尾那句話都不翻。
> 現在會翻了,而真正的外殼(logo、選單、版權行)照舊不動。
>
> 這比看起來大:37 站實測 The Verge 的漏字從 **60% 降到 0%**、
> AP News 從 20% 降到 0%、Rust 官網從 31% 降到 11%。
>
> **修**:圖片上的加註改成**毛玻璃 + 譯文貼片** —— 玻璃把原文糊掉但保留明暗,
> 所以你看得出底下是深色圖表還是白底截圖;譯文帶自己的米白底,在任何圖上
> 都讀得到。短標籤(`N/A` → 「不適用」)橫向長,不再折成兩行。
>
> **補**:小字的**圓點滑上去會出貼片**(譯文 + 原文),放大檢視右邊多一份
> **註解清單** —— 以前圓點只會放大 1.35 倍,什麼都讀不到。
>
> **修**:按住 <kbd>Alt</kbd> 看原文之後**放開,圖片的加註會回來** ——
> 以前它會就此消失,得滑走再滑回來才出現。放大檢視裡按住 <kbd>Alt</kbd>
> 現在也真的會把加註**掀起來**(那個黑窗一直印著這句話,但沒做)。
>
> **修**:網站在圖片上壓一層透明按鈕的(ClickHouse 那種點圖放大的),
> 現在**不用先點開**,直接滑上去就會翻。
>
> **修**:圖片翻譯不會再無聲卡住。背景服務被系統回收之後現在會自己醒回來
> (以前圖片那條路沒有掛喚醒,只能等你剛好又滑到一張圖上);每一段連線
> 都有上限時間,超過會告訴你是「抓不下來」還是「模型沒回應」,而不是
> 轉三分鐘之後說「沒有回應」;而且一個沒回應的請求不會再把**所有**的圖
> 一起卡住。跑到一半被系統回收的請求現在會**自動重來**,而不是要你
> 「滑開再滑回來」——你根本沒有滑開,你在等。

### 做法:加註,不是重繪

毛玻璃疊膜把原文收進一片亮帶、退到背景,譯文帶白色光暈浮在原位 ——
**不重繪、不配色、不裁切原圖**。按住 <kbd>Alt</kbd> 就掀開看原圖。

疊膜用的是 `contrast()` 而不是 `brightness()`,因為**深底和淺底都要有效**:
壓暗只在淺底上管用,深色圖表上原文會照樣讀得到、反而是譯文看不見。
四種底色(深、淺、中灰、彩)的可讀性由 `npm run check` 量著:
原文殘留 < 4:1、譯文 ≥ 4.5:1。
(第一版是取樣背景色畫不透明貼片,實測兩個死穴:配色永遠有例外、
小字縮到 11px 以下必糊,否決留檔。)

### 對稱律

文字是**譯文常駐、滑上去看原文**;圖片是**原圖常駐、滑上去看譯文**。
圖的資訊主體是圖形,整頁掛著標註色是噪音。

### 兩段式,和文字管線同構

- **滑上去停 0.5 秒** → 免費檔(零帳單,10–70 秒,背景填充)
- **<kbd>Alt</kbd>+點圖** → 升級到你選的檔位(截圖級約 NT$0.2)

「不按就不翻」在這裡鬆了一格:**自動的只能是免費的**。

### 字太小的變成編號 —— 而且**一張圖只用一種形式**

算出來小於 11px 的不硬塞,改成編號圓點,滑上去看譯文。
決定疊字還是編號的是**整張圖**(七成以上放得下就整張疊字,少數塞不下的
把框撐大),不是逐塊 —— 同一張圖上混著兩種形式,讀圖的人得同時維持
兩種閱讀模式。
圖角的「⤢ 放大檢視」點開之後那些字夠大了,**整張自動變回疊字** ——
同一份譯文換個尺寸重算,不會再花一次錢。站方自己有放大功能就不出那顆按鈕,
加註會跟著同一張圖過去。

### 零成本的那一半先做

mermaid / d3 圖表裡的 `<text>` 是**真的文字節點**,走既有的貼片管線,
零視覺模型成本。`<img alt>` 同理。

### 量出來的參數(不是猜的)

- 縮圖上限 2048:**輸入 token 完全不隨尺寸變**,縮圖一毛錢都省不到,
  而免費檔的召回會從 100% 掉到 61%
- 輸出格式跟著來源走:JPEG 照片轉 PNG 會膨脹九倍
- 詞表在圖片上是**請求不是保證**:0/3 → 2~3/3,而哪個模型比較聽話和價格無關

### 已知不行的

直排日文讀不對(模型把直排當橫排讀)—— 但認得出來,標警示色不假裝翻對。
`<canvas>` 的字、CSS 背景圖的字不翻。

### 品質

271 個測試(+54)、四支真瀏覽器 probe。`tests/fixtures/vision/` 收了
真實 API 回應當地面實況。走過的坑在 `docs/deviations.md` §DE–§DF。

## v0.1.0 — 文字階段收關

自用的 Chrome 疊層翻譯:譯文以不透明疊層覆蓋原文,**完全不改動頁面 DOM 與 CSS**。

### 這一版有什麼

- **兩段式翻譯**:L0(Chrome Translator API,本機、零成本)先填滿畫面,
  停留超過 1.5 秒的區塊才升級到 L1(Gemini / Gemma),就地 cross-fade 替換,
  不動幾何、不重算字級。
- **三層防守**:內文疊翻、UI 標籤走 hover / 選取貼片(不覆蓋原文)、
  `code` 與 `.notranslate` 一律不翻。沒有元素可掛的鬆散文字與被行內圖片
  切開的段落改用 `Range` 錨點,而且**疊層絕不蓋到圖**。
- **詞表**:`原文 → 譯法`,只寫原文 = 不翻。全域 + 具名詞表 + 網域 pattern
  綁定。做法是私用區佔位符 —— 模型從頭到尾沒看到那個詞,所以**每個檔位
  都有效,連 L0 也有效**。有自己的匯出匯入(和快取那組分開)。
- **成本閘門**:不按就不翻、停留才升級、每頁 token 上限、每日預算、
  token bucket、429 退避。thoughts 單獨計價列出。
- **看得見的失敗**:提示線階層色、頁內狀態列、診斷 log 一鍵匯出
  (API key 遮罩、字串截斷到 60 字)、L1 佇列看門狗讓「卡住」不是一個
  能永久停留的狀態。
- **匯出**:譯文快取(JSON)、疊好的頁面(HTML,hover / Alt 仍可看原文)。

### 品質

- 217 個測試 + 三支真瀏覽器 probe(選取規則、顏色解析、HTML 快照)。
- 37 個台灣讀者常看的美日內容站與 CS 技術站的批次偵測稽核
  (`npm run audit:sites`)當回歸測試。
- 走過的坑逐件記在 `docs/deviations.md`,歸納出的通則在 `docs/lessons.md`。

### 限制與下一步

- **圖片翻譯不做**,進場條件是文字階段連續用兩週。
- 導覽列、頁尾、表單裡的文字不畫常駐疊層(hover / 選取翻得到)—— 這是設計。
- 日文 / 韓文是「應該會動」而不是「測過了」,主力測試對象是英文。
- L0 只在桌機版 Chrome 138+ 且語言包裝得起來時存在;不支援會自動退回 `single`。

### 安裝

下載 zip、解壓,`chrome://extensions` → 開發人員模式 → 載入未封裝項目 → 選解壓出來的資料夾。
使用說明在擴充功能內的手冊(popup → 手冊)。
